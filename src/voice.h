#pragma once
#include <Arduino.h>

// Голосовой цикл Ясмины: кнопка → запись с INMP441 → POST на сервер диалога →
// разбор ответа → воспроизведение через MAX98357.
//
// Лицом отсюда не рулим напрямую из середины длинных операций: voice только
// выставляет состояние (face::setState / setSpeaking / setThinking), а рисует
// лицо своя задача на другом ядре — поэтому Ясмина живая и во время HTTP,
// который блокирует секунды.
namespace voice {

void begin();

// Один проход конечного автомата. Зовётся из loop(); внутри блокируется
// надолго (запись, запрос, проигрывание) — это нормально, лицо не здесь.
void tick();

// Что сейчас происходит — для лога.
const char *stageName();

} // namespace voice
